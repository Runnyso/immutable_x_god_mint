# Immutable X — God Mint Tracker

Detects when someone mints **10 000+ NFTs in a single transaction** on Immutable X (L2 for gaming).

This is not a normal drop.  
This is **creation at god-level scale**.

Only possible because IMX allows true batch minting with zero gas per NFT.

## Run

```bash
python immutable_x_god_mint.py
